# CHANGELOG

## [v8.1.2-hlfromz.1] - 2026-07-30

### 新增

- 新增 `灯类别`，提供开关、亮度、色温和切换动作的通用画像。
- 新增 `yeelink.light.lamp2`（米家台灯 Pro）精确型号画像：
  - 读取电源、亮度和色温；
  - 控制电源、`1~100` 亮度、`2500~4800 K` 色温及五种灯光模式；
  - 支持无参数 `toggle` 动作；
  - 不再把仅写模式或台灯规格不存在的开关模板属性当作实时状态读取。

### 维护

- 将插件仓库地址切换为 `HLfromZ/astrbot_plugin_mihome`，由个人 fork 提供后续更新。

## [v8.1.1] - 2026-07-24

### 修复

- 直接显示小米账号登录二维码，修复扫码后再次出现二维码的问题。
- WebUI 明确登录二维码的可用扫码入口与应用。
- 收窄“授权异常”判定，仅在明确鉴权失效时提示重新授权。

## [v8.1.0] - 2026-07-24

### 改进

- 设备管理页在顶部刷新按钮旁增加保存入口，手机端隐藏遮挡内容的底部保存栏。
- WebUI 主色调整为米家绿，并精简页面、配置提示与 Tool 返回文案。
- 场景 Tool 保存后采用服务端配置回填；WebUI 与 AstrBot 原生“插件设置”共用配置，并阻止旧页面覆盖新设置。
- 新增 `viomi.aircondition.y71` 精确画像，修复共享设备详情与动态动作调用，优化批量状态读取、云端超时、凭证及缓存清理。
- 收紧音箱跨设备动作边界，严格校验整数属性，并按 AstrBot 最新元数据规范移除失效平台声明。
- 为设备准备结果增加有界短缓存，复用同批控制的设备目录与规格解析；减少重复状态读写，并限制损坏备份与冷却记录占用。
- 登录轮询复用已显示的二维码，降低授权期间的响应体积；WebUI 按页面延迟加载 Tool 设置。
- 隔离登录轮询与设备状态抽屉的晚到响应，避免旧请求覆盖当前页面。
- 新增 `tools/maintenance`，保存上游核对、项目检查、性能基线与本次审计记录。
- README 恢复 v7.3.0 的标题、折叠指南、FAQ、Roadmap 与免责声明结构，在原内容上补充 v8 功能。

## [v8.0.0] - 2026-07-24

### 新增

- 新增安全隔离的四个设备控制 LLM Tool：
  - `list_mihome_devices`：列出控制白名单中的设备及静态能力概览。
  - `inspect_mihome_device`：检查静态可控能力与云端观测能力。
  - `control_mihome_device`：顺序设置静态画像允许的设备属性。
  - `call_mihome_action`：执行静态画像明确允许的设备动作。
- 新增 `control_tool` 配置组：总开关默认关闭、`admin_only` 默认开启、`allowed_devices` 默认空列表并失败关闭。
- 新增 `xiaomi.wifispeaker.oh2p`（小米智能音箱）精确型号画像，覆盖音量、静音、播放状态和经验证的媒体/文本动作。
- “米家管理”页面新增设备控制 Tool 的权限与白名单配置；页面仍只用于管理、只读查询和诊断。

### 安全与行为边界

- WebUI 不注册设备控制或场景执行接口，不提供遥控按钮和场景执行按钮。
- 设备控制 Tool 只接受 `device_map` 中由管理员明确加入白名单的精确别名，不向模型返回 DID。
- 未知型号的动态能力仅用于检查和适配诊断；可写白名单只采用精确型号画像，未知型号不开放 LLM 直接控制。
- `control_mihome_device` 单批最多 5 项，按顺序执行且非原子；已完成项目保留执行结果。
- 同一插件实例同时只允许一个设备控制任务；并发请求会失败关闭。同一设备的连续属性控制或动作设有 3 秒冷却。
- 带参动作只对经过精确型号验证的参数结构开放，不按动作名向其他型号扩权；日志和结果不回显敏感参数内容。
- 管理员限制只信任 AstrBot 官方管理员判定，群主或群管理员身份不参与 AstrBot 管理员权限计算。
- `/米家登录` 不再向聊天发送带票据的登录链接，统一引导至内置“米家管理”页面扫码，避免票据进入群聊和平台消息日志。
- 属性与动作结果区分“确认成功”“网关已接收但未确认（`unconfirmed`）”和“失败”；`mijiaAPI` 返回 `code=1` 时不再误报设备已执行成功。
- 场景 LLM Tool 不向模型返回家庭 ID；家庭信息仅在管理员管理界面和手动场景列表中按需展示。
- 主进程与登录沙盒均抑制 `mijiaAPI` 的 DEBUG 日志，避免上游将请求体、动作文本或认证数据写入 AstrBot 日志。
- 业务请求不再预先调用 `mijiaAPI.login()`，避免凭证失效时意外进入无法及时取消的扫码长轮询；同时移除设备构造前的重复列表拉取。

### 依赖与兼容

- 将 `mijiaAPI` 锁定到 `4.1.3`，并适配当前设备规格、读写标志与动作调用行为。
- 最低 AstrBot 版本保持 `4.24.2`，以支持 Plugin Pages 内置管理页面。
- 保留 `device_map`、`device_category_map`、只读 Tool 和场景 Tool 的现有配置；从 v7 升级无需重新绑定设备。
- 插件版本更新为 `v8.0.0`，同步运行时注册版本和发布元数据。

### 文档

- 重写 README，补充安装升级、v7→v8 迁移、三组 LLM Tool、WebUI 能力边界、安全隐私、排障和开发测试说明。
- 更新设备支持矩阵至 `2026-07-24`，区分型号画像、类别画像与只用于诊断的动态能力。
- 更新设备适配 Issue 模板，禁止提交 Token、Cookie、认证文件和 DID，仅接受人工检查并脱敏的规格资料。

### 贡献

- 感谢 [@Siq5005](https://github.com/Siq5005) 在 [PR #13](https://github.com/674537331/astrbot_plugin_mihome/pull/13) 中贡献设备能力聚合、四 Tool 设计、音箱适配与实机数据。
- v8 纳入上述贡献，并结合当前 `mijiaAPI`、AstrBot Tool 参数规范和真实家居安全边界进行了重构，**不是对 PR #13 的原样合并**。

## [v7.4.0] - 2026-07-23

### 新增

- 新增 AstrBot 内置“米家管理”页面，可从插件详情页直接打开。
- 新增米家账号概览与扫码登录流程，二维码由插件本地生成，支持在页面中查看授权进度和安全退出。
- 新增可视化设备管理：同步云端设备、搜索筛选设备，并编辑设备别名与类别映射。
- 新增已映射设备的只读实时状态抽屉，不提供任何控制入口。
- 新增场景缓存查看与同步、LLM Tool 权限配置，以及账号、缓存、映射和运行异常诊断。

### 改进

- 保存设备映射前展示变更摘要，并对别名、DID、类别和映射数量进行校验。
- 页面适配 AstrBot 明暗主题和窄屏布局，采用米家风格的橙色主色与卡片式界面。
- 原有聊天命令全部保留，可继续使用 `/米家登录`、`/刷新米家`、`/米家详情` 等流程。
- 插件版本更新至 `v7.4.0`，最低 AstrBot 版本调整为 `4.24.2`。
- 补充插件短描述与发布文件忽略规则，符合 AstrBot 当前的插件展示和发布建议。

### 安全

- WebUI 仅提供账号授权、配置、同步、只读状态和诊断能力，不注册设备控制或场景执行接口。
- 登录二维码及其链接仅在插件进程内存中短暂保存，不写入状态文件；二维码由插件本地生成，前端响应排除 Cookie、Token 与认证文件内容。
- 云端设备信息和诊断信息按白名单输出并进行脱敏处理。
- 退出登录和高风险 Tool 权限变更需要再次确认；建议场景 Tool 仅允许管理员调用。
- 场景 Tool 的管理员判定仅信任 AstrBot 管理员权限，不再把群主或群管理员身份提升为全局管理员。

### 兼容性

- 完整兼容旧版本 `device_map` 与 `device_category_map` JSON 映射，无需迁移或重新绑定设备。
- 保留 AstrBot 原生插件配置页中的 JSON 高级编辑入口，内置页面与原生配置共用同一份配置。
- Plugin Pages 依赖 AstrBot `>=4.24.2`；旧版 AstrBot 需要先升级才能显示“米家管理”。

### 文档

- README 快速上手改为内置页面流程，并补充页面能力边界、数据存储位置、旧配置兼容和页面排障说明。

## 设备支持

当前支持范围、安全边界与实测状态以[设备支持矩阵](docs/device_support_matrix.md)为准。设备、固件和地区存在差异，未列出的型号不承诺可直接控制。

---

## [v7.3.0] - 2026-07-16

### Added
- `/米家帮助` 不带参数时返回按账号、查询、场景和控制分类的完整指令总览。
- 新增 `/mihome_help` 与 `/mihomehelp` 英文别名。
- 为所有聊天命令补齐面向用户的说明，便于帮助插件自动收集。

### Changed
- `/米家帮助` 调整为只读公开入口，使其默认显示在 `astrbot_plugin_help` 的 `/helps` 菜单中；账号、设备、场景与控制命令仍保持管理员权限。
- `/米家帮助 [设备别名]` 继续提供原有的设备专属控制指南。
- 插件版本更新至 `v7.3.0`。

### Docs
- README 补充总帮助入口、英文别名，以及 `/helps` 的 `show_all_cmds` 配置说明。

## [v7.2.1] - 2026-04-26

### Added 参考 [Issue #8](https://github.com/674537331/astrbot_plugin_mihome/issues/8)
- 新增型号精确适配：
  - `xiaomi.router.r4av2`（小米路由器4A 千兆版）
- 新增路由器状态映射（只读）：
  - 下载/上传速率、连接设备数、上网方式、WiFi 信息、在线时长等
- 新增路由器动作映射：
  - `重启` -> `reboot`

### Changed
- `requirements.txt` 中将 `mijiaAPI` 依赖从 `>=1.0.0` 调整为 `==3.0.5`，锁定当前已验证版本，降低上游变更导致的兼容性波动风险。

### Docs
- 完善 `docs/device_support_matrix.md`：
  - 新增“适配层级”“适配来源”说明，明确区分型号精确适配、类别兜底、英文透传兜底。
  - 增加更新时间，提升矩阵可维护性与可追溯性。
  - 补充并规范路由器型号 `xiaomi.router.r4av2` 的来源标注（Issue #8）。

### Fixed
- 修复 `device_profiles.py` 中 `GLOBAL_DISPLAY_MAP` 的重复键 `auto_keep_warm`，避免字典构建时静默覆盖。
- 同步插件运行时注册版本号为 `7.2.1`，与 `metadata.yaml` 保持一致。

## [v7.2.0] - 2026-04-08

### Added
- 场景 LLM Tool 新增可选管理员限制开关：
  - `scene_tool.admin_only`
- 扫地机档位值映射补充 `0挡 -> 0`，用于更明确的拖地水量控制表达。

### Changed
- 将米家场景 LLM Tool 配置从平铺结构调整为分组结构：
  - `scene_tool.enable`
  - `scene_tool.admin_only`
- 场景类函数工具（缓存场景列表、执行场景）接入统一访问校验逻辑。
- README 中扫地机控制示例同步为 `拖地水量 0挡`。

### Security
- 当 `scene_tool.admin_only` 开启时，场景 LLM Tool 仅允许管理员调用，降低非授权场景触发风险。

### Compatibility
- 保留并隐藏旧配置键作为兼容兜底：
  - `enable_scene_tool`
  - `scene_tool_admin_only`
- 代码读取逻辑已兼容新旧结构，已有用户配置无需手动迁移。

## [v7.1.2] - 2026-04-07

### Added 参考 [PR #4](https://github.com/674537331/astrbot_plugin_mihome/pull/4)
- 新增设备类别：
  - 开关类别
  - 门磁传感器类别
  - 燃气传感器类别

新增设备属性值友好展示映射能力：
支持将设备原始状态值翻译为更易理解的中文展示文本
可用于只读状态展示与 /米家详情 输出

### Changed 参考 [PR #4](https://github.com/674537331/astrbot_plugin_mihome/pull/4)
- 优化 /米家详情 与只读状态输出逻辑：
- 在保留原有属性翻译基础上，新增对属性值的可读化翻译
- 提升门磁、燃气传感器等设备状态展示可读性

- 增强属性值翻译兼容性：
  - 新增更宽松的值翻译逻辑，兼容字符串、整数、浮点、布尔等不同返回类型
  - 减少米家接口返回类型不一致导致的展示异常

### Fixed 参考 [PR #4](https://github.com/674537331/astrbot_plugin_mihome/pull/4)
- 补充开关类设备值映射：
  - 支持将“开”映射为 True
  - 支持将“关”映射为 False

## [v7.1.1] - 2026-04-04

### Added
- `/刷新米家` 现在会同时展示设备 `DID` 与 `model`，便于：
  - 配置 `device_map`
  - 排查设备兼容性
  - 提交 `MODEL_PROFILES` 适配 Issue
- 新增设备类别：
  - `热水器类别`
  - `路由器类别`
- 新增型号精确适配：
  - `xiaomi.kettle.ym3pro`（米家恒温电水壶3Pro）
  - `xiaomi.router.r3600`（小米AIoT路由器AX3600）

### Changed
- 更新 README 中 `/刷新米家` 的输出说明，明确区分：
  - `DID`：用于填写 `device_map`
  - `model`：用于 `get_device_info()`、画像适配与问题排查
- 更新 `device_category_map` 说明文档，补充：
  - `热水器类别`
  - `路由器类别`
- 更新只读 Tool 文档与示例，补充热水壶、路由器场景说明。

### Fixed
- 修正文档中容易将 `DID` 与 `model` 混淆的问题。
- 修复 README 排障指引与实际 `/刷新米家` 输出不一致的问题。
- 修复 LLM Tool 对不存在场景胡言乱语的问题。
---
## [v7.1.0] - 2026-03-27
### Added
- 新增米家设备只读型 LLM Tool：
  - `list_configured_mihome_aliases`
  - `read_mihome_device_status_by_alias`
- 新增配置项 `enable_readonly_tool`，用于控制是否启用米家设备只读查询工具。
- 大模型现可基于**已配置别名**读取设备实时状态，例如温湿度、空气质量、电源状态、运行模式等。
### Changed
- 米家设备只读 Tool 明确采用**实时读取**策略，不使用缓存，避免将旧状态误判为当前状态。
- 只读 Tool 的访问范围被严格限制为：
  - 仅允许读取 `device_map` 中已配置别名的设备
  - 不允许读取未配置别名的设备
  - 不允许执行任何控制、动作或场景
### Security
- 这是一次面向家居安全与权限边界的增强更新。
- 大模型的设备读取能力被限制为“只读 + 别名白名单”，避免自由探测全屋设备。
- 只读 Tool 不开放底层透传控制能力，不开放任意设备写入能力。
---
## [v7.0.1 ] - 2026-03-14

- 增加了list_cached_mihome_scenes_tool，场景缓存在本地，使得大模型调用场景更加准确
  
## [v7.0.0 ] - 2026-03-14

- ⚠️**这是一份涉及家居安全性的更新**
- 增加了/米家场景列表 /米家场景 [名称或scene_id] 功能
- 增加了enable_scene_tool，允许大模型直接执行场景

## [v6.6.0 ] - 2026-03-14

- model 适配了米家扫拖机器人 5 Pro（xiaomi.vacuum.ov21cn）
- 类别适配了 扫地机大类
- 增加了动作指令

## [v6.5.0 ] - 2026-03-14

- 修改了设备读取菜单，现在通过model-大类-无类别进行降级处理。

## [v6.4.1 ] - 2026-03-13

- 修改了读取时间，改进了设备拉取稳定性

## [v6.4.0 ] - 2026-03-13

解决了米家云端设备已经被用户改名后，详情页画像失效、帮助不准确、未适配设备信息量不足等问题。

#### ✨ 新增
- 新增插件配置项 `device_category_map`
  - 采用“别名 -> 类别”的方式手动指定设备大类
  - 当前支持：
    - `空调类别`
    - `净化器类别`
    - `风扇类别`
    - `蒸煮锅类别`
    - `空气炸锅类别`
    - `无类别`
- 新增“原始能力菜单探测”兜底机制
  - 当设备未配置类别，或类别为 `无类别` 时
  - `/米家详情 [设备别名]` 将直接返回该设备支持的原始英文属性列表
- 新增“模板外原始属性补充显示”
  - 对于已配置类别的设备
  - 若探测到模板未覆盖的属性，会在详情页中额外列出，方便排障与后续适配

#### 🔧 调整
- `/刷新米家`
  - 现在统一展示为：
    - `【插件别名】(米家云端名称) [状态] (DID)`
  - 让列表展示更贴近米家 App 中的真实命名习惯
- `/米家详情`
  - 已配置有效类别时：
    - 优先展示中文化的“可调属性 / 状态传感”
    - 再尝试精准读取实时状态
  - 未配置类别时：
    - 不再硬套中文模板
    - 改为直接输出设备原始能力菜单
- `/米家帮助`
  - 现在优先根据你配置的设备类别输出更准确的帮助内容
  - 若设备为 `无类别`，则返回通用帮助，并提示可透传英文属性
- `/米家控制`
  - 控制格式保持不变：
    - `/米家控制 [设备别名] [属性] [值]`
  - 若设备已配置类别，则优先使用对应中文映射
  - 若设备未分类，仍支持直接透传原始英文属性名

#### 🛠 优化
- 优化了“别名 / 云端名称 / 控制模板”三者之间的职责分离：
  - 别名：用于机器人内控制入口
  - 云端名称：用于用户界面展示
  - 设备类别：用于中文模板匹配与帮助输出
- 降低了对设备 `name` 和 `model` 自动猜测的依赖，改为以用户手工配置类别为主
- 详情页在实时数据读取失败时，不再只返回“无数据”，而是尽量回退展示原始能力项

#### 🧯 修复
- 修复了设备在米家 App 中被改名后，详情页无法稳定命中原有中文模板的问题
- 修复了部分情况下 `/米家详情` 只剩默认开关信息、缺少有效排障线索的问题
- 改善了未适配设备的可观测性，避免“设备在线但什么都看不到”的体验

#### ⚠️ 配置迁移说明
本版本建议在保留原有 `device_map` 的基础上，新增配置 `device_category_map`。例如：

```json
{
  "空调": "空调类别",
  "净化器": "净化器类别",
  "风扇": "风扇类别",
  "蒸煮锅": "蒸煮锅类别",
  "空气炸锅": "空气炸锅类别",
  "手环": "无类别"
}
```

## [v6.3.11 ] - 2026-03-11

### ✨ 新特性 (New Features)

* **🚀 引入“千机千面”物模型架构 (Device Profiles)**
  * 彻底解耦“用户自定义别名”与“底层设备控制逻辑”。
  * 新增 `device_profiles.py` 独立配置库，通过官方名称特征词（如“空气净化器”、“落地扇”）自动挂载专属控制字典。
  * 轻松实现跨设备品类的自然语言控制（如：将人类语言的“睡眠”、“制冷”精准转译为底层所需的数字枚举）。
* **📊 全中文实况状态透传**
  * `/米家详情` 指令迎来史诗级视觉增强。
  * 自动嗅探设备公开属性，并内置黑名单过滤枯燥的工程级调试代码（如 `fault`, `dbg`, `crc32`）。
  * 将底层的英文传感器标识（如 `relative_humidity`, `filter_left_time`）自动翻译为优雅的全中文排版，并智能追加 `%`, `°C`, `天` 等单位。
* **🛠️ 开放式探索与共建引导**
  * 在 README 中加入标准的 Issue 提交模板，鼓励用户在使用 `/米家详情` 遇到未汉化的高级属性时提交数据，共建物模型图纸库。

### 🐛 修复 (Bug Fixes)

* 修复了并发修改持久化数据时的竞态风险（引入 `threading.RLock` 保护 `update_state`）。
* 修复了子进程退出和插件热重载 `terminate()` 时，底层会话生命周期管理混乱导致的悬空指针问题（缩小锁范围，仅用于引用交接）。
* 修复了指令前导空格处理不当导致的正则失效问题。
  
###  碎碎念 
* 让GPT优化了一下，代码已经完全看不懂也看不动了，测试下来是正常的就行。

---

## 🗺️ 未来更新计划 (Roadmap)


### 🌈 场景与自动化 (Smart Scenes)
* ~~**原生/本地场景支持**：支持调用米家 App 预设场景或在插件内自定义快捷指令组（如 `/米家场景 晚安`）。~~

### 🛡️ 体验与稳定性增强 (UX & Stability)
* **状态快照缓存**：即使设备离线，也能显示最后一次成功读取的状态信息（如“5分钟前湿度为 60%”）。
* **控制回读验证**：下发指令后自动异步回读设备状态，确保控制真正生效。
* **更友好的错误解释器**：将枯燥的系统错误码转译为人类易懂的排障建议。

### ⌨️ 管理与效率工具 (Management Tools)
* **别名管理命令化**：脱离 WebUI，直接通过指令 `/米家绑定`、`/米家解绑` 管理设备别名。
* **档案一键导出**：支持导出当前设备的完整物模型 JSON，方便开发者调试与用户反馈。

### 🧠 深度智能化 (Intelligence)
* ~~**自然语言模糊控制**：无需严格遵循指令格式，支持 `@机器人 把空调调到26度` 这种更自然的交互方式。~~
* **定时任务调度**：支持相对时间或绝对时间的控制预约（如“30分钟后关闭净化器”）。

### 🛠️ 关于Tool：
* 这是一个需要长期规划的事情，可能会做自然语言调取温度计之类的数据；大电器接入需优先评估安全风险，请**自行 fork**。

---
*如果您对以上功能有更好的建议，或有新的需求，欢迎提交 Issue！*
